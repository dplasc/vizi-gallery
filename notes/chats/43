U redu. Evo **STRUCTURED SUMMARY / HANDOFF** za **ovaj chat**, čisto, precizno i zaključano.

---

## 1. Chat broj

**Chat #43**

---

## 2. Naslov chata

**UI kontrast bug: Primary CTA nevidljiv (/app) + Dashboard CTA fix + Button standardizacija**

---

## 3. Faza projekta

**FAZA 42 — UI Stabilizacija & UX Bugfix (post-auth dashboard & app landing)**

---

## 4. Što smo konkretno napravili / zaključili

* Identificiran **kritični UI bug**:

  * Primary CTA (“Uredi profil”) na `/app` renderao se kao tirkizna traka **bez vidljivog teksta**
  * Secondary CTA (“Otvori moj profil”) na `/dashboard` bio **crno na crno**
* Utvrđeno da problem **nije** u:

  * Supabase Authu
  * Vercel deployu
  * cacheu
* Precizno razdvojeno:

  * `/app` ≠ `/dashboard` (dvije odvojene stranice, različiti fileovi)
* Standardiziran **Button component**:

  * Primary button **forciran na `text-white`**
  * Prekinuto oslanjanje na `primary-foreground`
* Riješen `/dashboard` CTA:

  * “Otvori moj profil” prebačen na `variant="outline"`
* Riješen `/app` CTA:

  * `Button asChild + Link` dobio **`!text-white` override** zbog Radix Slot class merge problema
* Dodan **komentar u kod** iznad CTA-a:

  * dokumentira zašto `!text-white` postoji
  * upozorava da se ne uklanja
* Svi fixevi:

  * commitani
  * pushani
  * potvrđeni na **Vercel Production (Ready)**

---

## 5. Što je IZRIČITO odlučeno / zaključano

* **Primary button = bijeli tekst (`text-white`)**
* Ne koristiti `text-primary-foreground` za CTA gumbe
* `Button` komponenta se **ne dira** bez novog UI-system chata
* `/app` i `/dashboard` se tretiraju kao **odvojeni UI entry pointi**
* `!text-white` na `/app` CTA je **namjerni i dozvoljeni override**
* Dodani komentar je **“do not touch” dokumentacija**

---

## 6. Otvorene stavke

* **Nema otvorenih bugova vezanih uz CTA gumbe**
* Font warning (Google Fonts / Geist) je **poznat i neblokirajući**
* Nema aktivnih regresija u auth flowu (OTP issue je bio stari link)

---

## 7. Sljedeći dogovoreni korak (samo jedan)

👉 **Odabrati sljedeći fokus nakon UI stabilizacije**
(npr. onboarding polish, pricing/planovi, share UX, ili pauza)

---

## 8. Napomena za nastavak u novom chatu

Novi chat započeti s kontekstom:

> **“Nakon FAZE 42.
> UI kontrast bugovi su riješeni i zaključani.
> Button i CTA ponašanje je stabilno.
> Idemo dalje s feature/UX razvojem.”**

Ovaj UI problem se **više ne otvara ponovno** bez eksplicitne odluke o promjeni design systema.
